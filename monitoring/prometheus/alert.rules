groups:
  - name: App
    rules:
      - alert: HighNotFound
        expr: 'sum by (space) (app_requests_total{path=~".+",status=~"404"}) > 0'
        labels:
          severity: page
        annotations:
          summary: Alert when any user hits a rate limit.
          
