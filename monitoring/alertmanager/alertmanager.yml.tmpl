global:
 resolve_timeout: 1m
 slack_api_url: '${slack_url}'

route:
 receiver: 'slack-notifications'

receivers:
- name: 'slack-notifications'
  slack_configs:
  - channel: '${slack_channel}'
    send_resolved: true
