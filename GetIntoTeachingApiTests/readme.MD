# Tests for the Get into Teaching Api

## Running the test locally
To run all tests locally you will require the Docker compose file to be running so the tests have access to;
- PostGres
- Redis

If you are running Jetbrains Rider and wish to ignore these test for speed, go to Settings> Build, Execution, Deployment | Unit Testing
then add Integration into the skip test from categories comma separated list.
